{
  "name": "mongo_job",
  "version": "0.0.1",
  "description": "Example of using mongo collection as a job queue.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Toby Ho",
  "license": "MIT",
  "dependencies": {
    "is-type": "0.0.1",
    "debug": "^0.8.1"
  },
  "devDependencies": {
    "async": "^0.8.0",
    "cmd-ln": "*",
    "mongodb": "*"
  }
}
